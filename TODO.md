# TODO

- [ ] Make `medicalequipment.html` responsive (header nav, hero section, callback form, equipment cards, footer)
  - [ ] Fix header layout for small screens (wrap items, responsive logo + call text)
  - [ ] Reduce horizontal overflow (avoid fixed paddings like `px-10`; avoid fixed widths like `w-full max-w`; use `px-4 sm:px-6`)
  - [ ] Make hero typography & spacing responsive
  - [ ] Ensure equipment cards grid stacks on mobile (`grid-cols-1`) and uses better spacing
  - [ ] Make callback form inputs full-width on mobile
  - [ ] Ensure footer grid collapses to single column and text scales
- [ ] Final test: check mobile widths (320/375/480) and confirm no horizontal scroll

